import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    padding: '24px',
  },
  authorLink: {
    display: 'inline-block',
    marginRight: '10px',
  },
}));
